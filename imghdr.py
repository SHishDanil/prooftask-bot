# imghdr.py — stub module for Python ≥3.13 (imghdr was removed)
# Если telegram.bot будет определять тип картинки, always return None

def what(file, h=None):
    """
    Точка входа для python-telegram-bot:
    возвращает None (не определяем тип изображения).
    """
    return None
